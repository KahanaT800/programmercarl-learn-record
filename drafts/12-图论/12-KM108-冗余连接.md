## KamaCoder 108. 冗余连接

- 链接：[KamaCoder 108. 冗余连接](https://kamacoder.com/problempage.php?pid=1181)
- 文章：[代码随想录](https://programmercarl.com/kamacoder/0108.冗余连接.html)
- 视频：[B站讲解](https://www.bilibili.com/video/BV1gRM3z9EwZ)
- 状态：⚠️

### 第一想法
核心思想：已经是同一个根下的两个节点不能再次相连（会成环）；

### 看完题解后的想法
--

### 实现中遇到的困难
--

### 代码

```cpp
#include <iostream>
#include <vector>
using namespace std;

vector<int> father;
// 初始化
void init(int n) {
    father.resize(n);
    for (int i = 0; i < n; i++) {
        father[i] = i;
    }
}
// 路径压缩 + 查找根
int find(int x) {
    if (father[x] != x) father[x] = find(father[x]);
    return father[x];
}
// 合并两个集合
void join(int v, int u) {
    int rootV = find(v);
    int rootU = find(u);
    if (rootV == rootU) return;
    father[rootU] = rootV;
}
// 判断是否在一个集合
bool isSame(int v, int u) {
    int rootV = find(v);
    int rootU = find(u);
    return rootV == rootU;
}

int main() {
    int n, s, t;
    cin >> n;
    init(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> s >> t;
        if (isSame(s, t)) {
            cout << s << " " << t << endl;
            return 0;
        } else {
            join(s, t);
        }
    }
}
```
